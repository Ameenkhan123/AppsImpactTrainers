<%= form_with(model: [:trainee, @task], local: true) do |f| %>
<% if task.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>
	<ul>
		<% task.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>
<div class="form-group-material">
	<%= f.text_field :title,:class =>"input-material", :id =>"register-username" %>
	<label for="register-username" class="label-material">Title</label>
</div>
<div class="form-group-material">
	<%= f.text_area :description, class: "ckeditor form-control input-material", :id =>"register-username"  %>
	<label for="register-username" class="label-material">Description</label>
</div>
<div class="actions">
	<%= f.submit %>
</div>
<% end %>
<script >
	$(document).ready(function(){
		$("ckeditor").ckeditor({

		});
	});
</script>